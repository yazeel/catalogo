<p id="notice"><%= notice %></p>



<table class="table">
  <thead class="thead-inverse">
  
    <tr>
      <th>Color</th>
      <th>Talla</th>
      <th>Modelo</th>
      <th>Cantidad</th>
      <th>Precio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pedidos.each do |pedido| %>
      <tr>
        <td><%= pedido.color %></td>
        <td><%= pedido.talla %></td>
        <td><%= pedido.modelo %></td>
        <td><%= pedido.cantidad %></td>
        <td><%= pedido.precio %></td>
        <td><%= link_to 'Show', pedido %></td>
        <td><%= link_to 'Edit', edit_pedido_path(pedido) %></td>
        <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pedido', new_pedido_path %>
<%= link_to 'New reporte', pedidos_reporte_path %>